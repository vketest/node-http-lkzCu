apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/vketest/node-http-lkzcu:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/vketest/node-http-lkzcu:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: node-http-lkzcu
        chartPath: charts/node-http-lkzcu
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/vketest/node-http-lkzcu"
          image.tag: "{{.DIGEST_HEX}}"
